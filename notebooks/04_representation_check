{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.13","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[{"sourceType":"datasetVersion","sourceId":18254279}],"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# Phase 3 · Step 4 — Representation & tokenizer contract check (CPU)\n!pip install -q \"transformers==4.43.4\" einops","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-07-22T13:17:47.676688Z","iopub.execute_input":"2026-07-22T13:17:47.677021Z","iopub.status.idle":"2026-07-22T13:18:02.766037Z","shell.execute_reply.started":"2026-07-22T13:17:47.676996Z","shell.execute_reply":"2026-07-22T13:18:02.765351Z"}},"outputs":[{"name":"stdout","text":"\u001b[2K     \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m43.7/43.7 kB\u001b[0m \u001b[31m562.0 kB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m \u001b[36m0:00:01\u001b[0m\n\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m9.4/9.4 MB\u001b[0m \u001b[31m20.5 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m00:01\u001b[0m00:01\u001b[0m\n\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m566.4/566.4 kB\u001b[0m \u001b[31m27.5 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m\n\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m3.6/3.6 MB\u001b[0m \u001b[31m40.9 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m00:01\u001b[0m:00:01\u001b[0m\n\u001b[?25h","output_type":"stream"}],"execution_count":1},{"cell_type":"code","source":"import pandas as pd, numpy as np, torch\n\nPQ  = \"/kaggle/input/datasets/lazarvelinov46/cancer-variant-peft-processed/variant_windows_1024_split_v2.parquet\"\nREV = \"7bce263b15377fc15361f52cfab88f8b586abda0\"\nBP  = 512          # training window (primary)\n\ndf = pd.read_parquet(PQ)\n\n# --- v2 attachment guard: fail loudly if this is really v1 ------------------\nexp = {\"train\": (434, 37241, 5309), \"val\": (155, 6450, 1125), \"test\": (40, 48328, 9284)}\nfor sp, (g, n, p) in exp.items():\n    s = df[df.split == sp]\n    got = (s.gene.nunique(), len(s), int(s.label.sum()))\n    assert got == (g, n, p), f\"{sp}: expected {(g,n,p)}, got {got} — WRONG DATASET VERSION\"\nprint(\"v2 attachment confirmed: val is 6,450 rows / 1,125 pos (17.4%)\")\n\n# --- ref_window integrity: does the base at `offset` equal REF? -------------\nbad = df[[w[o] != r for w, o, r in zip(df.ref_window, df.offset, df.ref)]]\nassert bad.empty, bad[[\"chrom\", \"pos\", \"ref\", \"alt\", \"offset\"]].head(10)\nprint(f\"ref_window[offset] == ref for all {len(df):,} rows\")\nprint(\"win_len distribution:\", df.win_len.value_counts().to_dict())\n\n# --- 512 bp centred crop + alt derivation ----------------------------------\ndef crop(seq, off, L=BP):\n    n = len(seq)\n    if n <= L:\n        return seq, off\n    start = max(0, min(off - L // 2, n - L))\n    return seq[start:start + L], off - start\n\ndef ref_alt(row, L=BP):\n    w, o = crop(row.ref_window, row.offset, L)\n    assert w[o] == row.ref\n    return w, w[:o] + row.alt + w[o + 1:]\n\nsample = df.sample(2000, random_state=0)\npairs = [ref_alt(r) for r in sample.itertuples()]\nassert all(len(a) == len(b) for a, b in pairs)\nlens = {len(a) for a, _ in pairs}\nprint(f\"\\ncrop -> {BP} bp | observed lengths: {lens} | ref/alt differ by exactly 1 base: \"\n      f\"{all(sum(x!=y for x,y in zip(a,b)) == 1 for a,b in pairs)}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-22T13:19:04.69469Z","iopub.execute_input":"2026-07-22T13:19:04.69497Z","iopub.status.idle":"2026-07-22T13:19:08.481797Z","shell.execute_reply.started":"2026-07-22T13:19:04.694947Z","shell.execute_reply":"2026-07-22T13:19:08.481027Z"}},"outputs":[{"name":"stdout","text":"v2 attachment confirmed: val is 6,450 rows / 1,125 pos (17.4%)\nref_window[offset] == ref for all 92,019 rows\nwin_len distribution: {1024: 92019}\n\ncrop -> 512 bp | observed lengths: {512} | ref/alt differ by exactly 1 base: True\n","output_type":"stream"}],"execution_count":2},{"cell_type":"code","source":"from transformers import AutoTokenizer, AutoModel\nfrom transformers.models.bert.configuration_bert import BertConfig\n\nNAME = \"zhihan1996/DNABERT-2-117M\"\ncfg  = BertConfig.from_pretrained(NAME, revision=REV)\ntok  = AutoTokenizer.from_pretrained(NAME, trust_remote_code=True, revision=REV)\nmdl  = AutoModel.from_pretrained(NAME, trust_remote_code=True, config=cfg, revision=REV).eval()\n\nprint(\"type_vocab_size:\", getattr(cfg, \"type_vocab_size\", None),\n      \"| max_position_embeddings:\", getattr(cfg, \"max_position_embeddings\", None))\nprint(\"special tokens:\", tok.special_tokens_map)\n\n# --- token budget for the concat representation ----------------------------\nenc = tok([a for a, _ in pairs], [b for _, b in pairs])\ntl = np.array([len(x) for x in enc[\"input_ids\"]])\nprint(f\"\\nconcat [CLS] ref [SEP] alt [SEP] token length: \"\n      f\"min {tl.min()} | p50 {int(np.percentile(tl,50))} | p95 {int(np.percentile(tl,95))} \"\n      f\"| p99 {int(np.percentile(tl,99))} | max {tl.max()}\")\nprint(\"keys emitted by tokenizer:\", list(enc.keys()))\n\n# --- BPE cascade: how far does a 1-base change perturb tokenization? -------\ndef cascade(a, b):\n    m = min(len(a), len(b)); L = 0\n    while L < m and a[L] == b[L]: L += 1\n    R = 0\n    while R < m - L and a[-1-R] == b[-1-R]: R += 1\n    return max(len(a), len(b)) - L - R\n\nra = tok([a for a, _ in pairs])[\"input_ids\"]\naa = tok([b for _, b in pairs])[\"input_ids\"]\nspans   = np.array([cascade(x, y) for x, y in zip(ra, aa)])\nlendiff = np.array([len(x) != len(y) for x, y in zip(ra, aa)])\nprint(f\"\\nBPE cascade (tokens perturbed by the single-base change):\")\nprint(f\"  identical tokenization : {(spans==0).mean():.1%}  <- variant invisible to the model!\")\nprint(f\"  1-2 tokens             : {((spans>0)&(spans<=2)).mean():.1%}\")\nprint(f\"  3-10 tokens            : {((spans>2)&(spans<=10)).mean():.1%}\")\nprint(f\"  >10 tokens             : {(spans>10).mean():.1%}   max {spans.max()}\")\nprint(f\"  ref/alt token COUNT differs: {lendiff.mean():.1%}\")\n\n# --- segment embeddings: trained, or dead weight? --------------------------\nemb = None\nfor path in [\"embeddings.token_type_embeddings\", \"bert.embeddings.token_type_embeddings\"]:\n    m = mdl\n    try:\n        for p in path.split(\".\"): m = getattr(m, p)\n        emb = m; break\n    except AttributeError: continue\n\nif emb is None:\n    print(\"\\nno token_type_embeddings module -> rely on [SEP] alone\")\nelse:\n    W = emb.weight.detach()\n    print(f\"\\ntoken_type_embeddings shape {tuple(W.shape)}\")\n    for i in range(W.shape[0]):\n        print(f\"  row {i}: L2 {W[i].norm():.4f} | std {W[i].std():.4f} | \"\n              f\"{'ALL ZERO' if W[i].abs().max() < 1e-8 else 'nonzero'}\")\n    if W.shape[0] > 1:\n        print(f\"  cosine(row0,row1): {torch.nn.functional.cosine_similarity(W[0], W[1], dim=0):.4f}\")\n\n# --- does the forward pass actually respond to token_type_ids? --------------\ne = tok(pairs[0][0], pairs[0][1], return_tensors=\"pt\")\nids = e[\"input_ids\"]\ntti = e.get(\"token_type_ids\")\nprint(\"\\ntokenizer emits token_type_ids:\", tti is not None,\n      \"| unique values:\", set(tti.flatten().tolist()) if tti is not None else None)\nif tti is None:                       # build segments manually from [SEP]\n    sep = tok.sep_token_id\n    first = (ids[0] == sep).nonzero()[0].item()\n    tti = torch.zeros_like(ids); tti[0, first + 1:] = 1\ntry:\n    with torch.no_grad():\n        h1 = mdl(input_ids=ids, token_type_ids=tti)[0]\n        h0 = mdl(input_ids=ids, token_type_ids=torch.zeros_like(tti))[0]\n    d = (h1 - h0).abs().max().item()\n    print(f\"max |Δ hidden| with vs without segment ids: {d:.3e} -> \"\n          f\"{'HONORED' if d > 1e-6 else 'IGNORED (use [SEP] only)'}\")\nexcept TypeError as ex:\n    print(\"forward rejects token_type_ids ->\", ex)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-22T13:21:46.666754Z","iopub.execute_input":"2026-07-22T13:21:46.666999Z","iopub.status.idle":"2026-07-22T13:21:48.463731Z","shell.execute_reply.started":"2026-07-22T13:21:46.66698Z","shell.execute_reply":"2026-07-22T13:21:48.463034Z"}},"outputs":[{"name":"stderr","text":"Some weights of BertModel were not initialized from the model checkpoint at zhihan1996/DNABERT-2-117M and are newly initialized: ['bert.pooler.dense.bias', 'bert.pooler.dense.weight']\nYou should probably TRAIN this model on a down-stream task to be able to use it for predictions and inference.\n","output_type":"stream"},{"name":"stdout","text":"type_vocab_size: 2 | max_position_embeddings: 512\nspecial tokens: {'unk_token': '[UNK]', 'sep_token': '[SEP]', 'pad_token': '[PAD]', 'cls_token': '[CLS]', 'mask_token': '[MASK]'}\n\nconcat [CLS] ref [SEP] alt [SEP] token length: min 172 | p50 208 | p95 225 | p99 231 | max 237\nkeys emitted by tokenizer: ['input_ids', 'token_type_ids', 'attention_mask']\n\nBPE cascade (tokens perturbed by the single-base change):\n  identical tokenization : 0.0%  <- variant invisible to the model!\n  1-2 tokens             : 70.0%\n  3-10 tokens            : 30.0%\n  >10 tokens             : 0.0%   max 8\n  ref/alt token COUNT differs: 26.5%\n\ntoken_type_embeddings shape (2, 768)\n  row 0: L2 1.6000 | std 0.0576 | nonzero\n  row 1: L2 0.0433 | std 0.0016 | nonzero\n  cosine(row0,row1): 0.0579\n\ntokenizer emits token_type_ids: True | unique values: {0, 1}\nmax |Δ hidden| with vs without segment ids: 4.861e+00 -> HONORED\n","output_type":"stream"}],"execution_count":4},{"cell_type":"code","source":"# Phase 3 · Step 5 (retry) — Head architecture + LoRA target discovery (CPU)\nimport torch, torch.nn as nn, json\nfrom transformers import AutoModelForSequenceClassification, AutoConfig\nfrom transformers.models.bert.configuration_bert import BertConfig   # stock, not remote\n\nraw = AutoConfig.from_pretrained(NAME, revision=REV, trust_remote_code=True)\nprint(\"auto_map:\", json.dumps(getattr(raw, \"auto_map\", {}), indent=2))\n\n# stock config -> matches the remote model class's declared config_class\ncfg_clf = BertConfig.from_pretrained(NAME, revision=REV, num_labels=2)\nclf = AutoModelForSequenceClassification.from_pretrained(\n    NAME, revision=REV, trust_remote_code=True, config=cfg_clf)\nprint(\"loaded class:\", type(clf).__name__, \"|\", type(clf).__module__)\n\nprint(\"\\n--- non-encoder submodules (the head) ---\")\nfor n, m in clf.named_modules():\n    if n and \"encoder.layer\" not in n and isinstance(m, (nn.Linear, nn.Tanh, nn.Dropout)):\n        shape = tuple(m.weight.shape) if hasattr(m, \"weight\") else \"\"\n        print(f\"  {n:55} {type(m).__name__:10} {shape}\")\n\nhead = [(n, p.numel()) for n, p in clf.named_parameters()\n        if not n.startswith((\"bert.embeddings\", \"bert.encoder\"))]\ntot = sum(p.numel() for p in clf.parameters())\nprint(f\"\\ntotal params {tot:,} | head params {sum(v for _, v in head):,}\")\nfor n, v in head:\n    print(f\"  {n:55} {v:>9,}\")\n\ne = tok(pairs[0][0], pairs[0][1], return_tensors=\"pt\", truncation=True, max_length=256)\ne[\"token_type_ids\"] = torch.zeros_like(e[\"input_ids\"])\nwith torch.no_grad():\n    out = clf(**e, labels=torch.tensor([1]))\nprint(f\"\\nlogits {tuple(out.logits.shape)} | loss {float(out.loss):.4f} | keys {list(out.keys())}\")\n\nfrom collections import defaultdict\nsfx = defaultdict(list)\nfor n, m in clf.named_modules():\n    if isinstance(m, nn.Linear) and \"encoder.layer\" in n:\n        sfx[n.split(\".\")[-1]].append(tuple(m.weight.shape))\nprint(\"\\n--- encoder Linear suffixes (LoRA target_modules candidates) ---\")\nfor k, v in sfx.items():\n    print(f\"  {k:12} x{len(v):>3}  shape {v[0]}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-22T13:22:08.082209Z","iopub.execute_input":"2026-07-22T13:22:08.082482Z","iopub.status.idle":"2026-07-22T13:22:09.119281Z","shell.execute_reply.started":"2026-07-22T13:22:08.082459Z","shell.execute_reply":"2026-07-22T13:22:09.118545Z"}},"outputs":[{"output_type":"display_data","data":{"text/plain":"configuration_bert.py: 0.00B [00:00, ?B/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"de24b3c60d9f4070a8a97ac530caebd2"}},"metadata":{}},{"name":"stdout","text":"auto_map: {\n  \"AutoConfig\": \"zhihan1996/DNABERT-2-117M--configuration_bert.BertConfig\",\n  \"AutoModel\": \"zhihan1996/DNABERT-2-117M--bert_layers.BertModel\",\n  \"AutoModelForMaskedLM\": \"zhihan1996/DNABERT-2-117M--bert_layers.BertForMaskedLM\",\n  \"AutoModelForSequenceClassification\": \"zhihan1996/DNABERT-2-117M--bert_layers.BertForSequenceClassification\"\n}\n","output_type":"stream"},{"name":"stderr","text":"Some weights of BertForSequenceClassification were not initialized from the model checkpoint at zhihan1996/DNABERT-2-117M and are newly initialized: ['bert.pooler.dense.bias', 'bert.pooler.dense.weight', 'classifier.bias', 'classifier.weight']\nYou should probably TRAIN this model on a down-stream task to be able to use it for predictions and inference.\n","output_type":"stream"},{"name":"stdout","text":"loaded class: BertForSequenceClassification | transformers_modules.zhihan1996.DNABERT-2-117M.7bce263b15377fc15361f52cfab88f8b586abda0.bert_layers\n\n--- non-encoder submodules (the head) ---\n  bert.embeddings.dropout                                 Dropout    \n  bert.pooler.dense                                       Linear     (768, 768)\n  bert.pooler.activation                                  Tanh       \n  dropout                                                 Dropout    \n  classifier                                              Linear     (2, 768)\n\ntotal params 117,070,082 | head params 592,130\n  bert.pooler.dense.weight                                  589,824\n  bert.pooler.dense.bias                                        768\n  classifier.weight                                           1,536\n  classifier.bias                                                 2\n\nlogits (1, 2) | loss 0.6498 | keys ['loss', 'logits', 'hidden_states']\n\n--- encoder Linear suffixes (LoRA target_modules candidates) ---\n  Wqkv         x 12  shape (2304, 768)\n  dense        x 12  shape (768, 768)\n  gated_layers x 12  shape (6144, 768)\n  wo           x 12  shape (768, 3072)\n","output_type":"stream"}],"execution_count":5}]}